apt_packages: ['python-pip', 'python-dev', 'postgresql']

rails_app_name: '{{ ansible_hostname }}'
postgres_db_password: '{{ rails_app_name }}'


postgres_users:
  - { name: '{{ rails_app_name }}', password: '{{ postgres_db_password }}' }
